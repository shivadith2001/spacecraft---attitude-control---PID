# Testing guide

## Prerequisites
- Python 3.10+

## Run the automated edge-case tests
From the repository root:
```bash
python -m unittest discover -s tests -p "test_*.py"
```

## What the tests cover
See `tests.txt` for the full edge-case list.
